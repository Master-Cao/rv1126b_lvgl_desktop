# lvgl_desktop

这是一个轻量级 LVGL 桌面壳模块，用于把多个算法封装成页面式嵌入式应用。

目录建议：

- `lvgl_desktop.c/.h`：桌面入口、状态栏、应用卡片、页面切换。
- `apps/`：每个算法应用的页面和事件逻辑，例如 OCR、目标检测、分类。
- `components/`：可复用 UI 组件，例如状态栏、结果面板、按钮组、相机预览控件。

当前版本先实现一个简易桌面：

- 顶部状态栏
- 算法应用卡片
- 应用详情占位页面
- 底部状态栏

